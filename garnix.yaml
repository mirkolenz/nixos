builds:
  exclude:
    # sandbox initialization failed: pattern serialization length 431536 exceeds maximum (65535)
    # failed to configure sandbox: pattern serialization length 431536 exceeds maximum (65535))
    - darwinConfigurations.*
  include:
    - "checks.x86_64-linux.*"
    - "checks.x86_64-darwin.*"
    - homeConfigurations.*
    - nixosConfigurations.*
