on:
  push:
    branches:
      - main
  pull_request:
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true
jobs:
  check-eval:
    permissions:
      contents: read
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: DeterminateSystems/determinate-nix-action@v3
        with:
          extra-conf: |
            accept-flake-config = true
            eval-cores = 0
      - name: nix flake check
        run: |
          nix flake check \
            --impure \
            --print-build-logs \
            --show-trace

  check-build-matrix:
    name: ${{ matrix.system }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - system: x86_64-linux
            os: ubuntu-24.04
          - system: aarch64-darwin
            os: macos-15
    steps:
      - uses: actions/checkout@v5
      - uses: DeterminateSystems/determinate-nix-action@v3
        with:
          extra-conf: |
            accept-flake-config = true
            keep-going = true
            eval-cores = 0
      - uses: cachix/cachix-action@v16
        with:
          name: mirkolenz
          authToken: ${{ secrets.CACHIX_TOKEN }}
          # pushFilter: "(-source$ | -github-actions-checks$)"
      - name: nix build
        run: |
          nix build \
            --print-build-logs \
            --show-trace \
            --no-link \
            '.#githubActionsChecks.${{ matrix.system }}.default'

  check-build:
    needs: check-build-matrix
    runs-on: ubuntu-latest
    if: always()
    steps:
      - run: |
          result="${{ needs.check-build-matrix.result }}"
          if [[ $result == "success" ]]; then
            exit 0
          else
            exit 1
          fi
