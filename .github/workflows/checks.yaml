on:
  push:
    branches:
      - main
  pull_request:
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true
jobs:
  check-eval:
    permissions:
      contents: read
    runs-on: namespace-profile-x64-linux
    steps:
      - uses: actions/checkout@v5
      - uses: DeterminateSystems/determinate-nix-action@v3
        with:
          extra-conf: |
            accept-flake-config = true
            eval-cores = 0
      - name: nix flake check
        run: |
          nix flake check \
            --impure \
            --print-build-logs \
            --show-trace

  check-build-matrix:
    name: check-build.${{ matrix.system }}
    runs-on: ${{ matrix.runner }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - system: x86_64-linux
            runner: namespace-profile-x64-linux
          - system: aarch64-darwin
            runner: namespace-profile-aarch64-darwin
    steps:
      - uses: actions/checkout@v5
      - uses: namespacelabs/nscloud-cache-action@v1
        with:
          path: /nix
      - uses: DeterminateSystems/determinate-nix-action@v3
        with:
          extra-conf: |
            accept-flake-config = true
            keep-going = true
            eval-cores = 0
      - uses: cachix/cachix-action@v16
        with:
          name: mirkolenz
          authToken: ${{ secrets.CACHIX_TOKEN }}
          # pushFilter: "(-source$ | -github-actions-checks$)"
      - name: nix build
        run: |
          nix build \
            --print-build-logs \
            --show-trace \
            --no-link \
            '.#githubActionsChecks.${{ matrix.system }}.default'
      - run: nix store gc

  check-build:
    needs: check-build-matrix
    runs-on: ubuntu-latest
    if: always()
    steps:
      - run: |
          result="${{ needs.check-build-matrix.result }}"
          if [[ $result == "success" ]]; then
            exit 0
          else
            exit 1
          fi
