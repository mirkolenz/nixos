{ ... }:
{
  plugins.dap = {
    enable = true;
  };
  plugins.dap-python = {
    enable = true;
  };
  plugins.dap-virtual-text = {
    enable = true;
  };
  plugins.dap-ui = {
    enable = true;
  };
}
